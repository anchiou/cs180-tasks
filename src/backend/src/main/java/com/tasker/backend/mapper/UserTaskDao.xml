<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tasker.backend.mapper.UserTaskDao">

    <insert id="addTask">
        INSERT INTO UserTasks (TaskID, TaskName, Status, Deadline, Priority, Description, ListID)
        VALUES (#{id}, #{name}, #{status}, #{deadline}, #{priority}, #{description}, #{listId});
    </insert>

    <update id="updateTask">
        UPDATE UserTasks SET
            TaskName = #{name},
            Status = #{status},
            Deadline = #{deadline},
            Priority = #{priority},
            Description = #{description}
        WHERE TaskID = #{id};
    </update>

    <delete id="deleteTask">
        DELETE FROM UserTasks WHERE TaskID = #{id};
    </delete>

</mapper>